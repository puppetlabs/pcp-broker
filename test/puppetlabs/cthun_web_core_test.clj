(ns puppetlabs.cthun-web-core-test
  (:require [clojure.test :refer :all]
            [puppetlabs.cthun-service :as hello-svc]
            [puppetlabs.cthun-web-core :refer :all]
            [ring.mock.request :as mock]))

(deftest cthun-web-test
  (testing "Not yet implemented"
    (is (= 1 1))))